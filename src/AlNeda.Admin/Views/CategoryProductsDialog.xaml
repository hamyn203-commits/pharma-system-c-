<Window x:Class="AlNeda.Admin.Views.CategoryProductsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="منتجات التصنيف" Height="600" Width="800"
        WindowStartupLocation="CenterOwner"
        AllowsTransparency="True" WindowStyle="None" Background="Transparent">

    <Border Background="{StaticResource PageBgBrush}" CornerRadius="24" BorderThickness="1.5" BorderBrush="{StaticResource BorderDefaultBrush}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" /> <!-- Header -->
                <RowDefinition Height="*" />    <!-- List -->
                <RowDefinition Height="Auto" /> <!-- Footer -->
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="{StaticResource HeaderBgBrush}" CornerRadius="24,24,0,0" Padding="32,24">
                <Grid>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding CategoryIcon}" FontSize="24" Margin="0,0,16,0"/>
                        <StackPanel>
                            <TextBlock Text="{Binding CategoryName}" FontSize="20" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                            <TextBlock Text="{Binding ProductCountText}" FontSize="12" Foreground="{StaticResource TextMutedBrush}"/>
                        </StackPanel>
                    </StackPanel>
                    <Button HorizontalAlignment="Left" VerticalAlignment="Top" Style="{StaticResource TransparentButtonStyle}" Command="{Binding CloseCommand}">
                        <TextBlock Text="&#xE8BB;" FontFamily="Segoe MDL2 Assets" FontSize="16" Foreground="{StaticResource TextMutedBrush}"/>
                    </Button>
                </Grid>
            </Border>

            <!-- Product List -->
            <Grid Grid.Row="1" Margin="32,24">
                <DataGrid ItemsSource="{Binding Products}" Style="{StaticResource ModernDataGrid}" 
                          ColumnHeaderStyle="{StaticResource ModernDataGridColumnHeader}"
                          CellStyle="{StaticResource ModernDataGridCell}"
                          RowStyle="{StaticResource ModernDataGridRow}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="الاسم" Binding="{Binding Name}" Width="*" />
                        <DataGridTextColumn Header="الكمية" Binding="{Binding Quantity}" Width="100" />
                        <DataGridTextColumn Header="السعر" Binding="{Binding Price, StringFormat=N0}" Width="120" />
                        <DataGridTemplateColumn Header="الحالة" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Background="{Binding IsActive, Converter={StaticResource BoolToStatusBrushConverter}}" CornerRadius="10" Padding="8,2">
                                        <TextBlock Text="{Binding IsActive, Converter={StaticResource BoolToStatusTextConverter}}" Foreground="White" FontSize="11" HorizontalAlignment="Center"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>

            <!-- Footer -->
            <Border Grid.Row="2" Padding="32,24" Background="{StaticResource HeaderBgBrush}" CornerRadius="0,0,24,24">
                <Button Content="إغلاق" Command="{Binding CloseCommand}" Style="{StaticResource SecondaryButtonStyle}" Width="120" HorizontalAlignment="Left"/>
            </Border>
        </Grid>
    </Border>
</Window>
