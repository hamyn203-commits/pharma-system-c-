<UserControl x:Class="AlNeda.Admin.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:behaviors="clr-namespace:AlNeda.Admin.Behaviors"
             xmlns:controls="clr-namespace:AlNeda.Admin.Controls"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             mc:Ignorable="d" 
             d:DesignHeight="1000" d:DesignWidth="1200">

    <UserControl.Resources>
        <!-- Card Hover Animations -->
        <Style x:Key="DashboardCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardBgBrush}" />
            <Setter Property="CornerRadius" Value="20" />
            <Setter Property="Padding" Value="24" />
            <Setter Property="BorderThickness" Value="1.5" />
            <Setter Property="BorderBrush" Value="#FF3D4552" />
            <Setter Property="Effect" Value="{StaticResource CardShadow}" />
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5" />
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1" />
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <EventTrigger RoutedEvent="MouseEnter">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="1.02" Duration="0:0:0.2" />
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1.02" Duration="0:0:0.2" />
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
                <EventTrigger RoutedEvent="MouseLeave">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="1" Duration="0:0:0.2" />
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1" Duration="0:0:0.2" />
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Style.Triggers>
        </Style>

        <!-- Metric Text Styles -->
        <Style x:Key="MetricLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}" />
            <Setter Property="Margin" Value="0,0,0,8" />
        </Style>
    </UserControl.Resources>

    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding LoadCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="32,24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" /> <!-- Header -->
                <RowDefinition Height="Auto" /> <!-- Cards -->
                <RowDefinition Height="Auto" /> <!-- Charts -->
                <RowDefinition Height="*" />    <!-- Tables/Alerts -->
            </Grid.RowDefinitions>

            <!-- ═══════════════════════════════════════════════════════════ -->
            <!-- HEADER                                                      -->
            <!-- ═══════════════════════════════════════════════════════════ -->
            <Grid Grid.Row="0" Margin="0,0,0,32">
                <StackPanel>
                    <TextBlock Text="لوحة التحكم" Style="{StaticResource PageTitle}" />
                    <TextBlock Text="{Binding CurrentDate}" Foreground="{StaticResource TextMutedBrush}" Margin="0,4,0,0" />
                </StackPanel>
                
                <StackPanel HorizontalAlignment="Left" Orientation="Horizontal">
                    <Border Background="{DynamicResource PrimaryGreenGlassBrush}" CornerRadius="12" Padding="12,6">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🛡️ آخر نسخة احتياطية:" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,0,8,0"/>
                            <TextBlock Text="{Binding LastBackup}" Foreground="{StaticResource PrimaryGreenBrush}" FontSize="12" FontWeight="Bold"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>

            <!-- ═══════════════════════════════════════════════════════════ -->
            <!-- CARDS SECTION (Skeleton or Real)                            -->
            <!-- ═══════════════════════════════════════════════════════════ -->
            <Grid Grid.Row="1" Margin="0,0,0,32">
                <!-- Skeleton View -->
                <UniformGrid Columns="3" Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <controls:SkeletonCard Margin="0,0,16,16" />
                    <controls:SkeletonCard Margin="8,0,8,16" />
                    <controls:SkeletonCard Margin="16,0,0,16" />
                    <controls:SkeletonCard Margin="0,0,16,0" />
                    <controls:SkeletonCard Margin="8,0,8,0" />
                    <controls:SkeletonCard Margin="16,0,0,0" />
                </UniformGrid>

                <!-- Data Cards -->
                <UniformGrid Columns="3" Visibility="{Binding IsLoading, Converter={StaticResource InverseBoolToVisibilityConverter}}">
                    
                    <!-- 1. Total Receivables -->
                    <Button Command="{Binding NavToReceivablesCommand}" Style="{StaticResource TransparentButtonStyle}">
                        <Border Style="{StaticResource DashboardCardStyle}" Margin="0,0,16,16" Background="{StaticResource ReceivablesGradient}">
                            <StackPanel>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="💰" FontSize="28" Grid.Column="0" VerticalAlignment="Top" Foreground="#FFFF5C5C"/>
                                    <TextBlock Text="إجمالي المستحقات" Style="{StaticResource MetricLabel}" Grid.Column="1" />
                                </Grid>
                                <TextBlock FontSize="42" FontWeight="Bold" Foreground="#FFFF6B6B" HorizontalAlignment="Right">
                                    <TextBlock.Effect>
                                        <DropShadowEffect BlurRadius="10" ShadowDepth="2" Color="Black" Opacity="0.5" />
                                    </TextBlock.Effect>
                                    <i:Interaction.Behaviors>
                                        <behaviors:AnimatedNumberBehavior TargetNumber="{Binding Stats.TotalReceivables}" />
                                    </i:Interaction.Behaviors>
                                </TextBlock>
                                <TextBlock Text="جنيه مصري" FontSize="14" FontWeight="SemiBold" Foreground="#BFFFFFFF" HorizontalAlignment="Right" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>
                    </Button>

                    <!-- 2. Active Orders -->
                    <Button Command="{Binding NavToActiveOrdersCommand}" Style="{StaticResource TransparentButtonStyle}">
                        <Border Style="{StaticResource DashboardCardStyle}" Margin="8,0,8,16" Background="{StaticResource OrdersGradient}">
                            <StackPanel>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="📦" FontSize="28" Grid.Column="0" VerticalAlignment="Top" Foreground="#FFD28A2D"/>
                                    <TextBlock Text="الطلبات النشطة" Style="{StaticResource MetricLabel}" Grid.Column="1" />
                                </Grid>
                                <TextBlock FontSize="42" FontWeight="Bold" Foreground="#FFE09D3A" HorizontalAlignment="Right">
                                    <TextBlock.Effect>
                                        <DropShadowEffect BlurRadius="10" ShadowDepth="2" Color="Black" Opacity="0.5" />
                                    </TextBlock.Effect>
                                    <i:Interaction.Behaviors>
                                        <behaviors:AnimatedNumberBehavior TargetNumber="{Binding Stats.ActiveOrders}" />
                                    </i:Interaction.Behaviors>
                                </TextBlock>
                                <TextBlock Text="قيد التنفيذ" FontSize="14" FontWeight="SemiBold" Foreground="#BFFFFFFF" HorizontalAlignment="Right" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>
                    </Button>

                    <!-- 3. Pharmacies -->
                    <Button Command="{Binding NavToPharmaciesCommand}" Style="{StaticResource TransparentButtonStyle}">
                        <Border Style="{StaticResource DashboardCardStyle}" Margin="16,0,0,16" Background="{StaticResource PharmaciesGradient}">
                            <StackPanel>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="🏥" FontSize="28" Grid.Column="0" VerticalAlignment="Top" Foreground="#FF55D66B"/>
                                    <TextBlock Text="الصيدليات المسجلة" Style="{StaticResource MetricLabel}" Grid.Column="1" />
                                </Grid>
                                <TextBlock FontSize="42" FontWeight="Bold" Foreground="#FF66E87A" HorizontalAlignment="Right">
                                    <TextBlock.Effect>
                                        <DropShadowEffect BlurRadius="10" ShadowDepth="2" Color="Black" Opacity="0.5" />
                                    </TextBlock.Effect>
                                    <i:Interaction.Behaviors>
                                        <behaviors:AnimatedNumberBehavior TargetNumber="{Binding Stats.TotalPharmacies}" />
                                    </i:Interaction.Behaviors>
                                </TextBlock>
                                <TextBlock Text="صيدلية مسجلة" FontSize="14" FontWeight="SemiBold" Foreground="#BFFFFFFF" HorizontalAlignment="Right" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>
                    </Button>

                    <!-- 4. Products -->
                    <Button Command="{Binding NavToProductsCommand}" Style="{StaticResource TransparentButtonStyle}">
                        <Border Style="{StaticResource DashboardCardStyle}" Margin="0,0,16,0" Background="{StaticResource ProductsGradient}">
                            <StackPanel>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="💊" FontSize="28" Grid.Column="0" VerticalAlignment="Top" Foreground="#FFE67E22"/>
                                    <TextBlock Text="إجمالي المنتجات" Style="{StaticResource MetricLabel}" Grid.Column="1" />
                                </Grid>
                                <TextBlock FontSize="42" FontWeight="Bold" Foreground="#FFF39C12" HorizontalAlignment="Right">
                                    <TextBlock.Effect>
                                        <DropShadowEffect BlurRadius="10" ShadowDepth="2" Color="Black" Opacity="0.5" />
                                    </TextBlock.Effect>
                                    <i:Interaction.Behaviors>
                                        <behaviors:AnimatedNumberBehavior TargetNumber="{Binding Stats.TotalProducts}" />
                                    </i:Interaction.Behaviors>
                                </TextBlock>
                                <TextBlock Text="صنف دوائي" FontSize="14" FontWeight="SemiBold" Foreground="#BFFFFFFF" HorizontalAlignment="Right" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>
                    </Button>

                    <!-- 5. Daily Sales -->
                    <Button Command="{Binding NavToDailySalesCommand}" Style="{StaticResource TransparentButtonStyle}">
                        <Border Style="{StaticResource DashboardCardStyle}" Margin="8,0,8,0" Background="{StaticResource DailySalesGradient}">
                            <StackPanel>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="📈" FontSize="28" Grid.Column="0" VerticalAlignment="Top" Foreground="#FF316DCA"/>
                                    <TextBlock Text="مبيعات اليوم" Style="{StaticResource MetricLabel}" Grid.Column="1" />
                                </Grid>
                                <TextBlock FontSize="42" FontWeight="Bold" Foreground="#FF4080DB" HorizontalAlignment="Right">
                                    <TextBlock.Effect>
                                        <DropShadowEffect BlurRadius="10" ShadowDepth="2" Color="Black" Opacity="0.5" />
                                    </TextBlock.Effect>
                                    <i:Interaction.Behaviors>
                                        <behaviors:AnimatedNumberBehavior TargetNumber="{Binding Stats.DailySales}" />
                                    </i:Interaction.Behaviors>
                                </TextBlock>
                                <TextBlock Text="جنيه مصري" FontSize="14" FontWeight="SemiBold" Foreground="#BFFFFFFF" HorizontalAlignment="Right" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>
                    </Button>

                    <!-- 6. Monthly Sales -->
                    <Button Command="{Binding NavToMonthlySalesCommand}" Style="{StaticResource TransparentButtonStyle}">
                        <Border Style="{StaticResource DashboardCardStyle}" Margin="16,0,0,0" Background="{StaticResource MonthlySalesGradient}">
                            <StackPanel>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="🏢" FontSize="28" Grid.Column="0" VerticalAlignment="Top" Foreground="#FF1ABC9C"/>
                                    <TextBlock Text="مبيعات الشهر" Style="{StaticResource MetricLabel}" Grid.Column="1" />
                                </Grid>
                                <TextBlock FontSize="42" FontWeight="Bold" Foreground="#FF2EE0BB" HorizontalAlignment="Right">
                                    <TextBlock.Effect>
                                        <DropShadowEffect BlurRadius="10" ShadowDepth="2" Color="Black" Opacity="0.5" />
                                    </TextBlock.Effect>
                                    <i:Interaction.Behaviors>
                                        <behaviors:AnimatedNumberBehavior TargetNumber="{Binding Stats.MonthlySales}" />
                                    </i:Interaction.Behaviors>
                                </TextBlock>
                                <TextBlock Text="مبيعات الشهر" FontSize="14" FontWeight="SemiBold" Foreground="#BFFFFFFF" HorizontalAlignment="Right" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>
                    </Button>
                </UniformGrid>
            </Grid>

            <!-- ═══════════════════════════════════════════════════════════ -->
            <!-- CHARTS SECTION                                              -->
            <!-- ═══════════════════════════════════════════════════════════ -->
            <Grid Grid.Row="2" Margin="0,0,0,32">
                <Border Style="{StaticResource CardStyle}" Height="450">
                    <Grid>
                        <TextBlock Text="تحليل المبيعات (آخر 30 يوم)" Style="{StaticResource SectionHeader}" Margin="24,20" />
                        <Border Margin="24,60,24,24">
                            <lvc:CartesianChart Series="{Binding SalesSeries}" 
                                                XAxes="{Binding XAxes}" 
                                                YAxes="{Binding YAxes}"
                                                TooltipPosition="Top" />
                        </Border>
                    </Grid>
                </Border>
            </Grid>

            <!-- ═══════════════════════════════════════════════════════════ -->
            <!-- ALERTS / RECENT ACTIVITIES                                  -->
            <!-- ═══════════════════════════════════════════════════════════ -->
            <Grid Grid.Row="3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="32" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- Stock Alerts -->
                <Border Grid.Column="0" Style="{StaticResource CardStyle}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Text="تنبيهات المخزون" Style="{StaticResource SectionHeader}" Margin="0,0,0,16" />
                        
                        <StackPanel Grid.Row="1">
                            <Button Command="{Binding ShowLowStockProductsCommand}" Style="{StaticResource TransparentButtonStyle}" Margin="0,0,0,8">
                                <Border Background="{DynamicResource WarningGlassBrush}" CornerRadius="12" Padding="16,12">
                                    <Grid>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="⚠️" FontSize="18" Margin="0,0,12,0"/>
                                            <TextBlock Text="منتجات مخزونها منخفض" Foreground="{StaticResource WarningBrush}" VerticalAlignment="Center"/>
                                        </StackPanel>
                                        <TextBlock Text="{Binding LowStockCount}" Foreground="{StaticResource WarningBrush}" FontWeight="Bold" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                                    </Grid>
                                </Border>
                            </Button>
                            
                            <Button Command="{Binding ShowExpiringProductsCommand}" Style="{StaticResource TransparentButtonStyle}">
                                <Border Background="{DynamicResource DangerGlassBrush}" CornerRadius="12" Padding="16,12">
                                    <Grid>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="⏰" FontSize="18" Margin="0,0,12,0"/>
                                            <TextBlock Text="منتجات تنتهي صلاحيتها قريباً" Foreground="{StaticResource DangerBrush}" VerticalAlignment="Center"/>
                                        </StackPanel>
                                        <TextBlock Text="{Binding ExpiringCount}" Foreground="{StaticResource DangerBrush}" FontWeight="Bold" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                                    </Grid>
                                </Border>
                            </Button>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- System Summary -->
                <Border Grid.Column="2" Style="{StaticResource CardStyle}">
                    <Grid>
                        <TextBlock Text="ملخص النظام" Style="{StaticResource SectionHeader}" Margin="0,0,0,16" />
                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                            <TextBlock Text="📈" FontSize="48" HorizontalAlignment="Center" Opacity="0.5"/>
                            <TextBlock Text="النظام يعمل بشكل مستقر" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,16,0,0"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </ScrollViewer>
</UserControl>