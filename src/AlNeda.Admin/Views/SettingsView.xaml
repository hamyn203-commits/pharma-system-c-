<UserControl x:Class="AlNeda.Admin.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="900" d:DesignWidth="1100">

    <UserControl.Resources>
        <!-- Tab Header Style -->
        <Style x:Key="SettingsTabItem" TargetType="TabItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="TabBorder" Padding="24,16" Margin="0,0,8,0" CornerRadius="16,16,0,0" Cursor="Hand">
                            <ContentPresenter x:Name="ContentSite" ContentSource="Header" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="TabBorder" Property="Background" Value="{StaticResource CardBgBrush}"/>
                                <Setter TargetName="TabBorder" Property="BorderBrush" Value="{StaticResource BorderDefaultBrush}"/>
                                <Setter TargetName="TabBorder" Property="BorderThickness" Value="1,1,1,0"/>
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                    <Condition Property="IsSelected" Value="False"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="TabBorder" Property="Background" Value="{StaticResource BorderMutedBrush}"/>
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <DataTemplate x:Key="TabHeaderTemplate">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding Icon}" FontSize="18" Margin="0,0,12,0" VerticalAlignment="Center"/>
                <TextBlock Text="{Binding Title}" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center"/>
            </StackPanel>
        </DataTemplate>
        
        <Style TargetType="GroupBox">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Margin" Value="0,0,0,24"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GroupBox">
                        <StackPanel>
                            <TextBlock Text="{TemplateBinding Header}" Style="{StaticResource SectionHeader}" Margin="0,0,0,16"/>
                            <Border Background="{StaticResource CardBgBrush}" CornerRadius="20" Padding="32,24" BorderBrush="{StaticResource BorderDefaultBrush}" BorderThickness="1.5">
                                <ContentPresenter />
                            </Border>
                        </StackPanel>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="32,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,32">
            <StackPanel>
                <TextBlock Text="الإعدادات المتقدمة" Style="{StaticResource PageTitle}" />
                <TextBlock Text="تخصيص تجربة الاستخدام وإدارة النظام" Foreground="{StaticResource TextMutedBrush}" Margin="0,4,0,0" />
            </StackPanel>
        </Grid>

        <!-- Tabs -->
        <TabControl Grid.Row="1" Background="Transparent" BorderThickness="0">
            <!-- 1. GENERAL -->
            <TabItem Style="{StaticResource SettingsTabItem}">
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📊" Margin="0,0,8,0"/>
                        <TextBlock Text="عام"/>
                    </StackPanel>
                </TabItem.Header>
                <ScrollViewer Margin="0,24,0,0">
                    <StackPanel MaxWidth="700" HorizontalAlignment="Left">
                        <GroupBox Header="المظهر واللغة">
                            <StackPanel>
                                <Grid Margin="0,0,0,24">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="24" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="لغة التطبيق" Style="{StaticResource CardTitle}" Margin="0,0,0,8"/>
                                        <ComboBox ItemsSource="{Binding Languages}" SelectedItem="{Binding CurrentSettings.Language}" Style="{StaticResource ModernComboBox}"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="الثيم المفضل" Style="{StaticResource CardTitle}" Margin="0,0,0,8"/>
                                        <ComboBox ItemsSource="{Binding Themes}" SelectedItem="{Binding CurrentSettings.Theme}" Style="{StaticResource ModernComboBox}"/>
                                    </StackPanel>
                                </Grid>

                                <Grid Margin="0,0,0,24">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="24" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="نوع الخط" Style="{StaticResource CardTitle}" Margin="0,0,0,8"/>
                                        <ComboBox ItemsSource="{Binding Fonts}" SelectedItem="{Binding CurrentSettings.FontFamily}" Style="{StaticResource ModernComboBox}"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="{Binding CurrentSettings.FontSize, StringFormat='حجم الخط ({0}px)'}" Style="{StaticResource CardTitle}" Margin="0,0,0,16"/>
                                        <Slider Minimum="12" Maximum="20" Value="{Binding CurrentSettings.FontSize}" TickFrequency="1" IsSnapToTickEnabled="True" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Grid>
                            </StackPanel>
                        </GroupBox>

                        <GroupBox Header="سلوك النظام">
                            <StackPanel>
                                <Border Background="{StaticResource BorderMutedBrush}" CornerRadius="12" Padding="20" Margin="0,0,0,16">
                                    <Grid>
                                        <StackPanel>
                                            <TextBlock Text="بدء التشغيل مع Windows" FontWeight="SemiBold" Margin="0,0,0,4"/>
                                            <TextBlock Text="سيتم فتح البرنامج تلقائياً عند تشغيل الجهاز" FontSize="11" Foreground="{StaticResource TextMutedBrush}"/>
                                        </StackPanel>
                                        <CheckBox IsChecked="{Binding CurrentSettings.StartWithWindows}" HorizontalAlignment="Left" VerticalAlignment="Center" Style="{StaticResource {x:Type CheckBox}}"/>
                                    </Grid>
                                </Border>

                                <Border Background="{StaticResource BorderMutedBrush}" CornerRadius="12" Padding="20">
                                    <Grid>
                                        <StackPanel>
                                            <TextBlock Text="التحديثات التلقائية" FontWeight="SemiBold" Margin="0,0,0,4"/>
                                            <TextBlock Text="التحقق من وجود إصدارات جديدة عند بدء التشغيل" FontSize="11" Foreground="{StaticResource TextMutedBrush}"/>
                                        </StackPanel>
                                        <CheckBox IsChecked="{Binding CurrentSettings.CheckUpdatesOnStartup}" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                                    </Grid>
                                </Border>
                            </StackPanel>
                        </GroupBox>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- 2. DATABASE -->
            <TabItem Style="{StaticResource SettingsTabItem}">
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="💾" Margin="0,0,8,0"/>
                        <TextBlock Text="قاعدة البيانات"/>
                    </StackPanel>
                </TabItem.Header>
                <ScrollViewer Margin="0,24,0,0">
                    <StackPanel MaxWidth="700" HorizontalAlignment="Left">
                        <GroupBox Header="اتصال البيانات">
                            <StackPanel>
                                <TextBlock Text="مسار ملف قاعدة البيانات" Style="{StaticResource CardTitle}" Margin="0,0,0,8"/>
                                <Grid Margin="0,0,0,24">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="12" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBox Text="{Binding CurrentSettings.DbPath}" IsReadOnly="True" Style="{StaticResource ModernTextBox}"/>
                                    <Button Grid.Column="2" Content="🔄 تغيير المسار" Command="{Binding BrowseDatabaseCommand}" Style="{StaticResource SecondaryButtonStyle}" Padding="20,0" Height="46"/>
                                </Grid>

                                <TextBlock Text="عنوان اتصال الخادم (API Base URL)" Style="{StaticResource CardTitle}" Margin="0,0,0,8"/>
                                <TextBox Text="{Binding CurrentSettings.ApiBaseUrl, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}" Margin="0,0,0,24" Height="46"/>

                                <Border Background="{StaticResource HeaderBgBrush}" CornerRadius="16" Padding="24" Margin="0,0,0,24">
                                    <Grid>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="📦" FontSize="24" Margin="0,0,16,0" VerticalAlignment="Center"/>
                                            <StackPanel>
                                                <TextBlock Text="حجم البيانات الحالي" Foreground="{StaticResource TextMutedBrush}" FontSize="12"/>
                                                <TextBlock Text="{Binding DbSize}" FontSize="20" FontWeight="Bold" Foreground="{StaticResource PrimaryBlueLightBrush}"/>
                                            </StackPanel>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                            <Button Content="🔌 اختبار الاتصال" Command="{Binding TestConnectionCommand}" Style="{StaticResource TransparentButtonStyle}" Foreground="{StaticResource PrimaryGreenBrush}" Margin="0,0,16,0"/>
                                            <Button Content="🧹 ضغط البيانات" Command="{Binding VacuumDbCommand}" Style="{StaticResource TransparentButtonStyle}" Foreground="{StaticResource WarningBrush}"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </StackPanel>
                        </GroupBox>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- 3. USERS -->
            <TabItem Style="{StaticResource SettingsTabItem}">
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="👤" Margin="0,0,8,0"/>
                        <TextBlock Text="المستخدمين"/>
                    </StackPanel>
                </TabItem.Header>
                <Grid Margin="0,24,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    
                    <Border Grid.Row="0" Style="{StaticResource CardStyle}" Padding="20" Margin="0,0,0,16">
                        <Grid>
                            <StackPanel>
                                <TextBlock Text="إدارة صلاحيات الوصول" FontSize="16" FontWeight="Bold"/>
                                <TextBlock Text="إضافة وتعديل حسابات الموظفين" FontSize="11" Foreground="{StaticResource TextMutedBrush}"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                <Button Content="➕ مستخدم جديد" Command="{Binding NewUserAsyncCommand}" Style="{StaticResource PrimaryButtonStyle}" Padding="24,0" Height="40"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <Border Grid.Row="1" Style="{StaticResource CardStyle}" Padding="0">
                        <DataGrid ItemsSource="{Binding Users}" SelectedItem="{Binding SelectedUser}"
                                  Style="{StaticResource ModernDataGrid}"
                                  ColumnHeaderStyle="{StaticResource ModernDataGridColumnHeader}"
                                  CellStyle="{StaticResource ModernDataGridCell}"
                                  RowStyle="{StaticResource ModernDataGridRow}">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="اسم المستخدم" Binding="{Binding Username}" Width="*" />
                                <DataGridTemplateColumn Header="الدور" Width="150">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Border Background="{DynamicResource PrimaryBlueGlassBrush}" CornerRadius="10" Padding="12,2" HorizontalAlignment="Left">
                                                <TextBlock Text="{Binding Role}" Foreground="{StaticResource PrimaryBlueLightBrush}" FontSize="12" FontWeight="Bold"/>
                                            </Border>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTextColumn Header="تاريخ الإنشاء" Binding="{Binding CreatedAt, StringFormat=\{0:yyyy/MM/dd\}}" Width="180" />
                                <DataGridTemplateColumn Header="الإجراءات" Width="120">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal">
                                                <Button Content="✏️" Command="{Binding DataContext.EditSelectedUserAsyncCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" Style="{StaticResource WindowButtonStyle}"/>
                                                <Button Content="🗑️" Command="{Binding DataContext.DeleteUserCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Style="{StaticResource CloseButtonStyle}" Margin="8,0,0,0"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>
                </Grid>
            </TabItem>

            <!-- 4. NOTIFICATIONS -->
            <TabItem Style="{StaticResource SettingsTabItem}">
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="🔔" Margin="0,0,8,0"/>
                        <TextBlock Text="الإشعارات"/>
                    </StackPanel>
                </TabItem.Header>
                <ScrollViewer Margin="0,24,0,0">
                    <StackPanel MaxWidth="700" HorizontalAlignment="Left">
                        <GroupBox Header="تنبيهات المخزون">
                            <StackPanel>
                                <Border Background="{StaticResource BorderMutedBrush}" CornerRadius="16" Padding="24" Margin="0,0,0,16">
                                    <StackPanel>
                                        <Grid Margin="0,0,0,16">
                                            <StackPanel>
                                                <TextBlock Text="تنبيه انتهاء الصلاحية" FontWeight="Bold"/>
                                                <TextBlock Text="إظهار تنبيه عند قرب انتهاء صلاحية الأدوية" FontSize="11" Foreground="{StaticResource TextMutedBrush}"/>
                                            </StackPanel>
                                            <CheckBox IsChecked="{Binding CurrentSettings.ExpiryAlertEnabled}" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                                        </Grid>
                                        <StackPanel Orientation="Horizontal" IsEnabled="{Binding CurrentSettings.ExpiryAlertEnabled}">
                                            <TextBlock Text="تنبيه قبل (يوم):" VerticalAlignment="Center" Margin="0,0,12,0"/>
                                            <TextBox Text="{Binding CurrentSettings.ExpiryDaysThreshold}" Width="100" Style="{StaticResource ModernTextBox}"/>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>

                                <Border Background="{StaticResource BorderMutedBrush}" CornerRadius="16" Padding="24">
                                    <StackPanel>
                                        <Grid Margin="0,0,0,16">
                                            <StackPanel>
                                                <TextBlock Text="تنبيه نفاد الكمية" FontWeight="Bold"/>
                                                <TextBlock Text="إظهار تنبيه عند وصول المخزون للحد الأدنى" FontSize="11" Foreground="{StaticResource TextMutedBrush}"/>
                                            </StackPanel>
                                            <CheckBox IsChecked="{Binding CurrentSettings.LowStockAlertEnabled}" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                                        </Grid>
                                        <StackPanel Orientation="Horizontal" IsEnabled="{Binding CurrentSettings.LowStockAlertEnabled}">
                                            <TextBlock Text="الحد الأدنى للكمية:" VerticalAlignment="Center" Margin="0,0,12,0"/>
                                            <TextBox Text="{Binding CurrentSettings.LowStockThreshold}" Width="100" Style="{StaticResource ModernTextBox}"/>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </GroupBox>

                        <GroupBox Header="طريقة التنبيه">
                            <StackPanel>
                                <TextBlock Text="تفضيلات الإشعارات" Style="{StaticResource CardTitle}" Margin="0,0,0,8"/>
                                <ComboBox ItemsSource="{Binding AlertMethods}" SelectedItem="{Binding CurrentSettings.AlertMethod}" Style="{StaticResource ModernComboBox}" Margin="0,0,0,16"/>
                                <CheckBox Content="تشغيل صوت عند التنبيه" IsChecked="{Binding CurrentSettings.PlaySoundOnAlert}" Foreground="{StaticResource TextPrimaryBrush}"/>
                            </StackPanel>
                        </GroupBox>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- 5. BACKUP -->
            <TabItem Style="{StaticResource SettingsTabItem}">
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="🛡️" Margin="0,0,8,0"/>
                        <TextBlock Text="النسخ الاحتياطي"/>
                    </StackPanel>
                </TabItem.Header>
                <ScrollViewer Margin="0,24,0,0">
                    <StackPanel MaxWidth="700" HorizontalAlignment="Left">
                        <GroupBox Header="الأمان والحفظ">
                            <StackPanel>
                                <Grid Margin="0,0,0,24">
                                    <StackPanel>
                                        <TextBlock Text="النسخ التلقائي" FontWeight="Bold"/>
                                        <TextBlock Text="حفظ نسخة احتياطية من البيانات دورياً" FontSize="11" Foreground="{StaticResource TextMutedBrush}"/>
                                    </StackPanel>
                                    <CheckBox IsChecked="{Binding CurrentSettings.AutoBackupEnabled}" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                                </Grid>

                                <StackPanel IsEnabled="{Binding CurrentSettings.AutoBackupEnabled}">
                                    <TextBlock Text="تكرار النسخ" Style="{StaticResource CardTitle}" Margin="0,0,0,8"/>
                                    <ComboBox ItemsSource="{Binding BackupIntervals}" SelectedItem="{Binding CurrentSettings.BackupInterval}" Style="{StaticResource ModernComboBox}" Margin="0,0,0,24"/>

                                    <TextBlock Text="مسار حفظ النسخ" Style="{StaticResource CardTitle}" Margin="0,0,0,8"/>
                                    <Grid Margin="0,0,0,24">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="12" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBox Text="{Binding CurrentSettings.BackupPath}" Style="{StaticResource ModernTextBox}"/>
                                        <Button Grid.Column="2" Content="📁 اختيار مجلد" Style="{StaticResource SecondaryButtonStyle}" Padding="20,0" Height="46"/>
                                    </Grid>
                                </StackPanel>

                                <Border Background="{DynamicResource DangerGlassBrush}" CornerRadius="16" Padding="24">
                                    <Grid>
                                        <StackPanel>
                                            <TextBlock Text="نسخة فورية" FontWeight="Bold" Foreground="{StaticResource DangerBrush}"/>
                                            <TextBlock Text="إنشاء نسخة احتياطية من البيانات الآن" FontSize="11" Foreground="{StaticResource DangerBrush}" Opacity="0.8"/>
                                        </StackPanel>
                                        <Button Content="🛡️ ابدأ النسخ الآن" Command="{Binding CreateBackupCommand}" Style="{StaticResource PrimaryButtonStyle}" HorizontalAlignment="Left" VerticalAlignment="Center" Background="{StaticResource DangerBrush}"/>
                                    </Grid>
                                </Border>
                            </StackPanel>
                        </GroupBox>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
        </TabControl>

        <!-- Actions -->
        <Border Grid.Row="2" Background="{StaticResource CardBgBrush}" CornerRadius="20" Padding="24" BorderBrush="{StaticResource BorderDefaultBrush}" BorderThickness="1.5">
            <Grid>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="ℹ️" FontSize="18" Margin="0,0,12,0" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding Status}" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                    <Button Content="🔄 إعادة تشغيل" Command="{Binding RestartAppCommand}" Style="{StaticResource TransparentButtonStyle}" Foreground="{StaticResource TextMutedBrush}" Margin="0,0,24,0"/>
                    <Button Content="🔄 افتراضي" Command="{Binding ResetDefaultsCommand}" Style="{StaticResource TransparentButtonStyle}" Foreground="{StaticResource TextMutedBrush}" Margin="0,0,24,0"/>
                    <Button Content="✅ حفظ الإعدادات" Command="{Binding SaveCommand}" Style="{StaticResource PrimaryButtonStyle}" Width="200" Height="46" FontWeight="Bold"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
