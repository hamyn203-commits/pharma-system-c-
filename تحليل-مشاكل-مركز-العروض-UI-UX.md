# تحليل وتوصيات UI/UX — صفحة "مركز العروض" (Offers Center)

> **المشروع:** مخزن الندا — نظام إدارة مخزن أدوية  
> **التقنية:** WPF + MVVM على .NET 9  
> **تاريخ التحليل:** ٢٧ مايو ٢٠٢٦  
> **الملف المحلل:** `src/AlNeda.Admin/Views/OffersView.xaml` (٧٠٠ سطر)

---

## أولاً: تشخيص دقيق لكل مشكلة من المشاكل الخمس

---

### 🔴 المشكلة الأولى: تداخل الكروت الإحصائية عند تصغير الشاشة

** السبب الجذري:**

الكروت الإحصائية (StatCards) معروضة داخل `ItemsControl` يستخدم `WrapPanel` كلوحة عناصر، وهذا صحيح من حيث المبدأ. لكن كل كرت له عرض ثابت (`Width="175"`) مع حد أدنى (`MinWidth="150"`)، مما يسبب:

1. **التفاف مبكر جداً عند عرض نافذة أقل من ~950px** — لأن `WrapPanel` يضع 4 كروت بعرض 175px + هوامش 10px = ~740px، ثم أي مساحة أقل تؤدي للتفاف الكرت الرابع تحت.
2. **عند الالتفاف، الصف الجديد لا يُحاذي بشكل جيد** — لأنه لا توجد محاذاة شبكية موحدة، وكل كرت له `Margin` منفصل.
3. **المشكلة تتفاقم مع التخطيط ثلاثي الأعمدة** — لأن العمود الأوسط (`*` بعرض متغير) يأخذ المساحة المتبقية، فعند تصغير النافذة، المساحة المتاحة للكروت في أعلى الصفحة تنكمش بشكل غير متوقع.

**موضع التداخل بالضبط:** السطور 83-114 في `OffersView.xaml` (الصف 1 من الشبكة الرئيسية).

**تأثير مرئي:** عندما تكون النافذة بعرض أقل من ١٠٠٠ بكسل تقريباً، الكرت الرابع (أو الثالث والرابع) ينتقل لصف جديد ويتداخل مع المحتوى أسفله، أو يظهر بشكل مقطوع جزئياً.

---

### 🔴 المشكلة الثانية: مساحات فارغة كبيرة بلا فائدة في منطقة المعاينة وأزرار غير متناسقة

**السبب الجذري:**

منطقة المعاينة (Preview) في العمود الرابع (Grid.Column="4") تعاني من عدة مشاكل هيكلية:

1. **هاتف وهمي صغير في مساحة كبيرة:** الهاتف الوهمي `MaxWidth="270"` ومحاذي للأعلى (`VerticalAlignment="Top"`) بينما العمود كاملاً عرضه بين 240px و 380px — مما يترك فراغات ضخمة حول الهاتف من الأسفل والجوانب. (السطور 623-690)

2. **اختيار وضع المعاينة (`ComboBox`) فوق هيكل الهاتف:** ثم الهاتف، ثم `StatusMessage` في الأسفل — هذه كلها عناصر رأسية تترك فراغات كبيرة بينها عند وجود نصوص قصيرة أو عدم وجود نصوص. (السطور 605-697)

3. **الأزرار العلوية في رأس الصفحة:** أزرار "عرض جديد" و"تحديث" و"تصدير" موضوعة في `WrapPanel`، لكن لا توجد معالجة للالتفاف أو للازدحام. عند تصغير النافذة، الأزرار تلتف فوق بعضها أو تختفي جزئياً. (السطور 71-78)

4. **زر "تغيير الصورة" (رفع صورة):** موجود في قسم "الصورة والوصف" داخل المحرر، وعرضه 100px فقط. في `DockPanel` مع العنوان، قد يظهر مقطوعاً أو غير واضح. (السطر 466-468)

---

### 🔴 المشكلة الثالثة: محتوى مخفي أو مقطوع

**السبب الجذري:**

1. **زر "رفع صورة":** عرضه ثابت 100px، والنص العربي "رفع صورة" يحتاج مساحة ~75-80px. المشكلة ليست في الزر نفسه، بل في `DockPanel` المحيط — فعندما يكون العنوان طويلاً، الـ `DockPanel` يضغط الزر وقد يقطعه. (السطور 463-468)

2. **نصوص إرشادية تطول وتختفي:** 
   - النص الإرشادي للمقاسات المطلوبة (`TextWrapping="Wrap"` موجود له) لكنه في `StackPanel` بدون `ScrollViewer` مخصص، مما يجعله يُضغط عند نقص المساحة الرأسية. (السطور 472-474)
   - عناوين العروض في القائمة اليسرى بها `TextTrimming="CharacterEllipsis"` و `MaxHeight="38"` — وهذا حل جزئي لكنه يقطع العناوين الطويلة جداً ويخفي محتوى قد يكون مهماً. (السطور 181-184)

3. **حقل "ملاحظات داخلية":** ارتفاعه 56px فقط، وهو صغير جداً لملاحظات قد تكون مهمة. (السطر 501-503)

4. **جدول تفاعل الصيدليات:** `MaxHeight="160"` مع 8 أعمدة — عند عرض 3-4 صفوف فقط، يظهر شريط تمرير أفقي/عمودي وقد يخفي بيانات. (السطور 538-558)

---

### 🔴 المشكلة الرابعة: نصوص غير احترافية في منطقة المعاينة

**السبب الجذري:**

من خلال تحليل ملف XAML، النص غير الاحترافي الذي ذُكر ("هذه الصفحة للمعاينة فقط. لا توجد تحويل تحرير كثيرة هنا.") **غير موجود في ملف OffersView.xaml الحالي**. هذا يعني أنه إما:

1. موجود في ViewModel (`OffersViewModel.cs`) كقيمة افتراضية لـ `StatusMessage` أو خاصية مشابهة.
2. موجود في إصدار أقدم من الملف وتم تعديله.

لكن تظل هناك نصوص تحتاج تحسين في المعاينة الحالية:

- **`TextBlock` في رأس المعاينة:** "معاينة التطبيق" — عنوان عام وغير واصف. الأفضل: "كيف يبدو العرض في تطبيق الصيدلي".
- **النص الوهمي الثابت "صيدلية الندى":** موجود كهارد كود في السطر 633 — هذا يفترض أن جميع الصيدليات تسمى "صيدلية الندى" وهو غير دقيق.
- **النص الوهمي للمعاينة:** `PreviewMode` المعروض كاسم صيدلية فرعي (السطر 635-637) — غير مفهوم للمستخدم.

**التوصية:** حتى لو أُزيل النص السيئ، المحتوى الوهمي الثابت يحتاج لاستبداله ببيانات حقيقية أو على الأقل نصوص عربية طبيعية.

---

### 🔴 المشكلة الخامسة: ازدحام التخطيط ثلاثي الأعمدة عند تصغير النافذة

**السبب الجذري:**

التخطيط الحالي في الصف 2 (السطور 117-124) يعتمد على:

```
العمود 0: قائمة العروض — Width="280" MinWidth="220"
العمود 1: GridSplitter — Width="Auto"
العمود 2: المحرر — Width="*" MinWidth="400"
العمود 3: GridSplitter — Width="Auto"
العمود 4: المعاينة — Width="300" MinWidth="240" MaxWidth="380"
```

**المشكلة الهيكلية:** مجموع الحدود الدنيا = 220 + 0 + 400 + 0 + 240 = **860px**، بالإضافة لهوامش الصفحة (16+16=32px) = **892px**. هذا يعني أن النافذة تحتاج عرض 900px على الأقل لمنع التداخل. عند عرض أقل من ذلك:

1. **الأعمدة تبدأ في التضارب** ولا يوجد `ScrollViewer` أفقي في هذا المستوى.
2. **العمود الأوسط (المحرر)** يختنق أولاً لأن عرضه `*` (نسبي)، فينكمش حتى يصل `MinWidth=400` ثم يتوقف — لكن العمودين الآخرين ثابتين، مما يسبب تجاوز المساحة المتاحة.
3. **الـ GridSplitter** لا يحل المشكلة بل يخفيها — المستخدم قد يسحب الفواصل لكن هذا حل مؤقت وغير بديهي.

**لا توجد استجابة حقيقية للتصغير** — التخطيط مصمم لشاشات 1080p فأكثر، ويتحطم تحت 1366x768 (وهي شائعة جداً في الصيدليات والصيدليات الصغيرة).

---

## ثانياً: حلول تصميمية مقترحة لكل مشكلة

---

### ✅ حل المشكلة الأولى: الكروت الإحصائية المتجاوبة

**التوصيات:**

1. **استبدال `WrapPanel` بـ `UniformGrid` ذي أعمدة متجاوبة:**
   - اجعل عدد الأعمدة يتغير حسب عرض النافذة باستخدام `AdaptiveTrigger` أو بربطه بخاصية في ViewModel.
   - عرض ≥ 1200px → 4 أعمدة
   - عرض 900-1199px → 3 أعمدة
   - عرض 600-899px → عمودين
   - عرض < 600px → عمود واحد
   
   هذا يضمن توزيعاً متساوياً دائماً بدون تداخل.

2. **توحيد عرض الكروت:**
   - اجعل كل كرت يتمدد ليملأ عرض العمود بدلاً من عرض ثابت 175px. استخدم `HorizontalAlignment="Stretch"` داخل الـ `UniformGrid`.

3. **إزالة الهوامش الجانبية المتضاربة:**
   - الهوامش الحالية (`Margin="0,0,10,10"` للسطر 92) تسبب عدم محاذاة. استخدم `Padding` على لوحة العناصر بدلاً من `Margin` على كل عنصر.

4. **إضافة `ScrollViewer` أفقي كحل أخير:**
   - إذا كان عدد الكروت 5 أو أكثر، فكر في جعل الصف الإحصائي قابلاً للتمرير أفقياً بدلاً من الالتفاف.

---

### ✅ حل المشكلة الثانية: المساحات الفارغة في المعاينة والأزرار

**التوصيات:**

1. **إعادة تصميم منطقة المعاينة بالكامل:**
   - اجعل الهاتف الوهمي يتمدد عمودياً (`VerticalAlignment="Stretch"` أو `VerticalAlignment="Center"`) بدلاً من `Top`.
   - أضف مساحة فارغة مقصودة (padding موحد 16px) بدلاً من فراغات عشوائية.
   - انقل `ComboBox` اختيار وضع المعاينة إلى شريط عنوان داخل الهاتف الوهمي نفسه، مما يقلل العناصر الرأسية المنفصلة.

2. **شريط الأزرار العلوي:**
   - استخدم `ToolBar` أو `StackPanel` أفقي مع `FlowDirection="RightToLeft"`.
   - عند تصغير النافذة، استخدم نمط "الأيقونة فقط" مع Tooltip بدلاً من إخفاء الأزرار.
   - أو: انقل الأزرار الثانوية (تحديث، تصدير) إلى قائمة منسدلة (...) لتوفير المساحة.

3. **منطقة المعاينة الفارغة:**
   - أضف محتوى مفيداً في المساحات الفارغة: ملخص سريع للإحصاءات، أو "حالة النشر" بشكل بطاقة صغيرة، أو تلميحات سياقية.
   - القاعدة: "كل بكسل فارغ بلا هدف = إهدار لانتباه المستخدم".

4. **زر "رفع صورة":**
   - استخدم `Grid` بدلاً من `DockPanel` لتجنب ضغط العناصر.
   - اجعل الزر بعرض `Auto` وليس ثابت 100px.
   - أضف أيقونة كاميرا (📷) + نص "اختر صورة" أو "تغيير" فقط ليكون أقصر.

---

### ✅ حل المشكلة الثالثة: المحتوى المخفي أو المقطوع

**التوصيات:**

1. **زر "رفع صورة":**
   - ضعه في صف منفصل أو استخدم `Grid` بأعمدة مرنة.
   - النص: "تغيير الصورة" ← "تغيير" (أقصر) أو "📷 تغيير".
   - العرض: `Auto` بدلاً من `Width="100"`.

2. **النصوص الإرشادية الطويلة:**
   - نص المقاسات المطلوبة: اجعله في `Expander` قابل للطي، يظهر فقط عند الحاجة.
   - أو: استخدم تنسيق نقطي (Bullet Points) بدلاً من جملة طويلة.
   - تأكد من `TextWrapping="Wrap"` ومن وجود `ScrollViewer` يغلف القسم كاملاً.

3. **قائمة العروض (العمود الأيسر):**
   - استبدل `TextTrimming="CharacterEllipsis"` مع `MaxHeight=38` بـ `MaxHeight=52` (سطرين كاملين) مع `TextTrimming="WordEllipsis"` (إن كان مدعوماً في WPF، أو استخدم Converter مخصص).
   - أضف Tooltip يعرض العنوان الكامل عند التمرير بالفأرة.

4. **حقل "ملاحظات داخلية":**
   - ارفع الارتفاع إلى 80px على الأقل.
   - أضف `ScrollViewer` داخلي أو `AcceptsReturn="True"` مع `VerticalScrollBarVisibility="Auto"`.

5. **جدول تفاعل الصيدليات:**
   - ارفع `MaxHeight` إلى 280px أو اجعله `Auto` داخل `ScrollViewer`.
   - اجعل الجدول قابلاً للتمرير أفقياً مع `HorizontalScrollBarVisibility="Auto"`.
   - فكر في إخفاء الأعمدة الأقل أهمية (مثل "الهاتف") عند عرض النافذة الضيق.

---

### ✅ حل المشكلة الرابعة: النصوص غير الاحترافية

**التوصيات:**

1. **إزالة أي نصوص "توضيحية للمطور" نهائياً:**
   - افحص ViewModel (`OffersViewModel.cs`) وتأكد من عدم وجود نصوص مثل "هذه الصفحة للمعاينة فقط".
   - في وضع عدم اختيار عرض: اعرض رسالة مفيدة مثل "اختر عرضاً من القائمة لمعاينته" أو "أنشئ عرضاً جديداً للبدء".

2. **تحسين النصوص الوهمية (Placeholder):**
   - "صيدلية الندى" ← اربطها باسم الصيدلية الفعلي من بيانات المستخدم إن أمكن، أو استخدم "معاينة العرض".
   - أضف تسمية توضيحية صغيرة: "هذه معاينة تقريبية — المظهر النهائي قد يختلف حسب إصدار التطبيق".
   - استخدم لغة تسويقية: "هكذا سيظهر عرضك للصيدليات" بدلاً من نصوص تقنية.

3. **مراجعة جميع النصوص في الصفحة:**
   - تأكد من أن كل نص له قيمة للمستخدم النهائي (مدير المخزن أو المسوق).
   - استخدم لغة عربية فصيحة ومهنية.
   - تجنب الاختصارات التقنية (مثل "Banner 1920×700") واستبدلها بنص مفهوم: "صورة رئيسية (عرض كامل)" مع إمكانية عرض التفاصيل التقنية عند الحاجة.

4. **معالجة حالة "لا توجد عروض":**
   - النص الحالي "أنشئ أول عرض ترويجي الآن" جيد، لكن أضف زر "إنشاء عرض" في منتصف الحالة الفارغة لتوجيه المستخدم مباشرة.

---

### ✅ حل المشكلة الخامسة: التخطيط ثلاثي الأعمدة المزدحم

**اقتراح بديل للتخطيط كاملاً (أنظر القسم الخامس للتفاصيل):**

1. **نمط "التبويب الأفقي" (Tab-Based):**
   - بدلاً من 3 أعمدة جنباً إلى جنب، استخدم `TabControl` أفقي بثلاثة تبويبات:
     - تبويب 1: "📋 قائمة العروض" (مع الفلاتر والبحث)
     - تبويب 2: "✏️ تحرير العرض" (المحرر كاملاً)
     - تبويب 3: "📱 معاينة" (المعاينة بشكل موسع)
   - هذا يمنح كل قسم المساحة الكاملة للنافذة، ومناسب جداً للشاشات الصغيرة.

2. **نمط "القائمة المنبثقة" (List-Detail with Overlay):**
   - تظهر قائمة العروض في لوحة جانبية قابلة للطي (مثل Outlook).
   - عند اختيار عرض، يفتح المحرر في المساحة الرئيسية.
   - زر "معاينة" يفتح نافذة منبثقة (Popup/Dialog) تحاكي الهاتف بحجم أكبر.

3. **إذا أصررت على التخطيط ثلاثي الأعمدة:**
   - أضف `ScrollViewer` أفقي على كامل الصف 2 لتفعيل التمرير عند التصغير تحت 900px.
   - استخدم `AdaptiveTrigger` مع `VisualStateManager` لإخفاء عمود المعاينة تلقائياً عند عرض < 1000px واستبداله بزر "عرض المعاينة" الذي يفتحها كنافذة منبثقة.
   - أو: قلل `MinWidth` لكل عمود: العمود الأيسر 180px، المحرر 320px، المعاينة 200px (بدلاً من 220/400/240).

---

## ثالثاً: مبادئ عامة لتصميم واجهة "مركز العروض"

---

### 🎨 ١. Dark Theme متسق

- **استخدم موارد الألوان المركزية:** كل الألوان يجب أن تشير إلى `{StaticResource ...}` وليس ألوان هارد كود. حالياً الملف به ألوان مثل `#1822363E` و `#141E293B` و `#55111E32` مبعثرة — هذه يجب توحيدها.
- **تدرج الألوان:** 
  - الخلفية العميقة: `PageBgBrush` (موجود)
  - سطح البطاقات: `CardBgBrush` (موجود)
  - سطح مرتفع (Hover/Selected): `GlassCardBrush` (موجود)
  - حدود خفيفة: `BorderDefaultBrush` (موجود)
- **لا تستخدم ألواناً من خارج الثيم أبداً.** كل قيمة لونية يجب أن تكون معرفة في `DarkTheme.xaml`.

### 🎯 ٢. RTL سلس

- **كل `FlowDirection="RightToLeft"` موجود بالفعل** — هذا ممتاز.
- **تأكد من:** الأيقونات لا تنعكس بشكل خاطئ (بعض أيقونات Segoe MDL2 Assets قد تحتاج تدوير).
- **الأرقام والتواريخ:** حافظ على عرضها من اليسار لليمين حتى في سياق RTL (استخدم `FlowDirection="LeftToRight"` على عناصر التاريخ والعملة).
- **اتجاه السحب (Swipe/Drag):** الـ `GridSplitter` الحالي حيادي ويعمل في كلا الاتجاهين بشكل صحيح.

### 📐 ٣. لا توجد عناصر مخفية أو مقطوعة

- **قاعدة `MinHeight` و `MinWidth` الذهبية:** كل عنصر تفاعلي يجب ألا يقل عن 32x32 بكسل (معيار الوصول Accessibility).
- **اختبار التصغير:** اختبر الصفحة عند 1024x768 و 1366x768 — هذان المقاسان شائعان جداً في الصيدليات.
- **النصوص:** استخدم `TextWrapping="Wrap"` بشكل افتراضي على كل `TextBlock` يحتمل أن يكون طويلاً.
- **أشرطة التمرير:** تأكد من ظهور `ScrollViewer` تلقائياً عند الحاجة (`Visibility="Auto"`).
- **الفيض (Overflow):** أي حاوية قد تحتوي محتوى أكبر من مساحتها = أضف `ScrollViewer` أو `ClipToBounds="True"` بشكل صريح.

### 🏢 ٤. إحساس احترافي "مركز تسويق"

- **التسلسل البصري (Visual Hierarchy):**
  - المستوى 1: رأس الصفحة + الكروت الإحصائية (لمحة سريعة).
  - المستوى 2: قائمة العروض + المحرر (منطقة العمل).
  - المستوى 3: المعاينة + التحليلات (تقييم النتائج).
  
- **شريط تقدم "خطوات النشر":** الخطوات الخمسة الموجودة أعلى المحرر فكرة ممتازة. عززها بـ:
  - أيقونة ✅ عند إكمال الخطوة.
  - لون مميز للخطوة الحالية.
  - إمكانية النقر على أي خطوة للانتقال السريع.

- **فحص الجاهزية (Readiness Checks):** فكرة ممتازة. اجعلها:
  - تظهر بشكل بطاقة عائمة أعلى المحرر عند وجود مشاكل.
  - تستخدم ألوان: أخضر (جاهز)، أصفر (تنبيه)، أحمر (مفقود).
  - تظهر عدد المشكلات المتبقية: "⚠️ تبقت 3 خطوات قبل النشر".

- **لغة الـ Microcopy:**
  - استخدم لغة مسوق محترف، وليس لغة مطور.
  - مثال: "نسبة التحويل" ← "معدل تحويل المشاهدات إلى طلبات".
  - مثال: "الاستهداف" ← "استهداف الصيدليات المستهدفة".
  - أضف تلميحات سياقية (Tooltips) بجانب كل حقل يشرح وظيفته.

- **الفراغ الأبيض (Negative Space):**
  - لا تخف من ترك مساحة فارغة بقصد — المساحة الفارغة المصممة جيداً تُحسّن القراءة والتركيز.
  - لكن المساحة الفارغة العشوائية = إهدار.

---

## رابعاً: ترتيب الأولويات

| الأولوية | المشكلة | لماذا | الجهد المتوقع |
|---------|---------|-------|---------------|
| 🔴 **حرج — أولاً** | المشكلة الخامسة: ازدحام التخطيط ثلاثي الأعمدة | تؤثر على كل مستخدم شاشته أقل من 1080p (أغلب الصيدليات) | 1-2 يوم |
| 🔴 **حرج — ثانياً** | المشكلة الأولى: تداخل الكروت الإحصائية | مرئية جداً وتؤثر على الانطباع الأول للصفحة | 0.5 يوم |
| 🟡 **عالي — ثالثاً** | المشكلة الرابعة: النصوص غير الاحترافية | سهلة الإصلاح، تأثيرها كبير على الاحترافية | 0.5 يوم |
| 🟡 **عالي — رابعاً** | المشكلة الثالثة: المحتوى المخفي أو المقطوع | تؤثر على إنتاجية المستخدم وقد تخفي معلومات مهمة | 1 يوم |
| 🟢 **متوسط — خامساً** | المشكلة الثانية: المساحات الفارغة في المعاينة | تحسين تجريبي وليس عائقاً وظيفياً | 1-2 يوم |

### خطة التنفيذ المقترحة:

**الجولة الأولى (يومان):**
1. إعادة هيكلة التخطيط ثلاثي الأعمدة إلى نمط متجاوب (المشكلة ٥).
2. إصلاح تداخل الكروت الإحصائية (المشكلة ١).
3. تنظيف النصوص غير الاحترافية (المشكلة ٤).

**الجولة الثانية (يومان):**
4. معالجة المحتوى المخفي والمقطوع (المشكلة ٣).
5. تحسين المساحات الفارغة وتصميم المعاينة (المشكلة ٢).

---

## خامساً: اقتراح بديل للتخطيط الحالي (ثلاثة أعمدة)

---

### 🏆 البديل الموصى به: نمط "القائمة الرئيسية + مساحة عمل موسعة" مع معاينة منبثقة

هذا النمط مستوحى من أدوات التسويق الاحترافية مثل **Mailchimp** و **HubSpot** و **Stripe Dashboard**:

```
┌──────────────────────────────────────────────────────────┐
│  🏷️ مركز العروض                      [+ عرض جديد] [⚙️]   │
│  صمم عروضك، استهدف صيدلياتك، وتابع الأثر مباشرة          │
├──────────────────────────────────────────────────────────┤
│                                                          │
│  ┌──────────────┐  ┌──────────────────────────────────┐  │
│  │              │  │                                  │  │
│  │  📋 العروض    │  │   ✏️ محرر العرض                   │  │
│  │              │  │                                  │  │
│  │  🔍 [بحث]   │  │   ┌─ الخطوات ──────────────────┐ │  │
│  │              │  │   │ ① أساسي ✓  ② منتجات  ③ ... │ │  │
│  │  ● عرض ١    │  │   └────────────────────────────┘ │  │
│  │  ● عرض ٢ 🔵 │  │                                  │  │
│  │  ● عرض ٣    │  │   [بيانات العرض]                 │  │
│  │  ● عرض ٤    │  │   [المنتجات المرتبطة]            │  │
│  │              │  │   [الاستهداف + فحص الجاهزية]     │  │
│  │  ─────────── │  │   [الصورة والوصف]               │  │
│  │  📊 إحصائيات │  │   [تحليلات]                      │  │
│  │  ┌────────┐  │  │                                  │  │
│  │  │ 12.5%  │  │  │   [نشر] [مسودة] [مراجعة] ...    │  │
│  │  │ تحويل  │  │  │                                  │  │
│  │  └────────┘  │  └──────────────────────────────────┘  │
│  │  ┌────────┐  │                                        │
│  │  │ 340    │  │     📱 [معاينة في تطبيق الصيدلي]        │
│  │  │ طلبات  │  │          (زر يفتح نافذة منبثقة)         │
│  │  └────────┘  │                                        │
│  │  ┌────────┐  │                                        │
│  │  │ 1.2K   │  │                                        │
│  │  │ ضغطات  │  │                                        │
│  │  └────────┘  │                                        │
│  │  ┌────────┐  │                                        │
│  │  │ 3.4K   │  │                                        │
│  │  │ مشاهدات│  │                                        │
│  │  └────────┘  │                                        │
│  │              │                                        │
│  └──────────────┘  └──────────────────────────────────┘  │
│                                                          │
└──────────────────────────────────────────────────────────┘
```

### مزايا هذا التصميم:

1. **عمودين فقط** بدلاً من 3 — أقل ازدحاماً وأكثر وضوحاً.
2. **القائمة اليسرى (280-320px):** تحتوي على قائمة العروض + الكروت الإحصائية عمودياً داخلها — مما يوفر مساحة أفقية كبيرة ويُبقي الإحصائيات مرئية دائماً.
3. **المحرر (المساحة المتبقية):** يتمدد ليملأ ~65% من العرض — مساحة واسعة ومريحة للعمل.
4. **المعاينة:** تفتح كنافذة منبثقة (Modal/Popup) عند النقر على زر "📱 معاينة" — مما يسمح بمشاهدتها بالحجم الكامل أو بحجم هاتف واقعي. هذا يحل مشكلة المساحة الفارغة جذرياً.
5. **متجاوب طبيعياً:** عند تصغير النافذة تحت 900px، القائمة اليسرى تنطوي (Collapse) إلى أيقونات فقط، ويأخذ المحرر كامل العرض.

### عند تصغير النافذة (< 900px):

```
┌──────────────────────────────────────┐
│  🏷️ [+ عرض] [💾] [📱] [⚙️]          │
├──────────────────────────────────────┤
│                                      │
│  ≡ ≡ ≡ ≡  ← أيقونات القائمة فقط      │
│  📋                                │
│  ➕                                │
│  📊                                │
│                                      │
│  ┌──────────────────────────────┐   │
│  │  ✏️ محرر العرض (كامل)         │   │
│  │                              │   │
│  │  [بيانات العرض]              │   │
│  │  [المنتجات]                   │   │
│  │  [الاستهداف]                  │   │
│  │  [تحليلات]                    │   │
│  │                              │   │
│  │  [نشر] [مسودة] [مراجعة]      │   │
│  └──────────────────────────────┘   │
│                                      │
└──────────────────────────────────────┘
```

---

### 🥈 البديل الثاني: نمط التبويبات (Tabs) — الأسهل تنفيذاً

إذا كان الوقت محدوداً، هذا أسرع بديل:

```
┌──────────────────────────────────────────────┐
│  🏷️ مركز العروض            [+ عرض جديد] [⚙️]  │
├──────────────────────────────────────────────┤
│  [📋 قائمة العروض] [✏️ تحرير] [📱 معاينة]     │ ← تبويبات أفقية
├──────────────────────────────────────────────┤
│                                              │
│  (محتوى التبويب النشط بكامل العرض)           │
│                                              │
│  ┌──────────────────────────────────────┐   │
│  │ تبويب 1: قائمة + فلاتر + إحصائيات     │   │
│  │ تبويب 2: محرر كامل بلا أعمدة جانبية   │   │
│  │ تبويب 3: معاينة بحجم كبير + تحليلات   │   │
│  └──────────────────────────────────────┘   │
│                                              │
└──────────────────────────────────────────────┘
```

**مزايا هذا النمط:**
- تنفيذ سريع (نصف يوم عمل).
- كل قسم يحصل على المساحة الكاملة.
- لا توجد مشاكل ازدحام أو تداخل.
- تبويب المعاينة يمكن أن يُظهر الهاتف الوهمي بحجم أكبر وأوضح.

**عيبه:** لا يمكن رؤية المحرر والمعاينة معاً — لكن هذا قد لا يكون ضرورياً، ويمكن تعويضه بزر "تحديث المعاينة" في المحرر ينتقل تلقائياً لتبويب المعاينة.

---

## الخلاصة النهائية

صفحة "مركز العروض" الحالية مبنية بشكل جيد من الناحية البرمجية (MVVM، ربط بيانات، أنماط منظمة)، لكنها تعاني من مشاكل تصميمية تؤثر على تجربة المستخدم، خاصة عند أحجام الشاشات الصغيرة والمتوسطة.

**التحركات الأساسية المطلوبة (حسب الأولوية):**

| # | الإجراء | التأثير |
|---|---------|---------|
| 1 | إعادة هيكلة التخطيط من 3 أعمدة إلى نمط متجاوب (عمودين + معاينة منبثقة) | يحل 80% من المشاكل دفعة واحدة |
| 2 | إصلاح الكروت الإحصائية باستخدام UniformGrid متجاوب | يزيل التداخل تماماً |
| 3 | تنظيف النصوص واستبدال النصوص الوهمية بلغة تسويقية احترافية | يرفع الاحترافية فوراً |
| 4 | مراجعة كل عنصر بيانات للتأكد من عدم وجود محتوى مخفي | يحسن الإنتاجية |
| 5 | تحسين توزيع المساحات في المعاينة وإضافة محتوى مفيد | لمسة نهائية احترافية |

**النتيجة المتوقعة بعد التطبيق:** صفحة "مركز العروض" ستتحول من "نموذج إدخال بيانات مزدحم" إلى "مركز تسويقي احترافي" ينافس أدوات التسويق الحديثة، ويعمل بسلاسة على جميع أحجام الشاشات المستخدمة في الصيدليات السعودية.

---

> ✍️ **تم إعداد هذا التحليل بواسطة Claude (خبير UI/UX)** بناءً على فحص كامل لملف `OffersView.xaml` والمستندات المرجعية للمشروع.